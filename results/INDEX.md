# Index of `results/`

Generated by `scripts/results_index.py` from each run's own artifacts. Do not edit by hand.

`validity then` is the run's verdict under the rules in force when it was made. `validity now` is `scripts/recheck_runs.py` re-judging it under the current, stricter rules. Where those differ, the difference is the point — the numbers were not re-measured, the standard moved.


## Runs behind published numbers

| run | stage | n | C | wall s | fail | $/query | q/s | ticker | topics | validity then | now |
|---|---|---|--:|--:|--:|--:|--:|--:|--:|:--:|:--:|

## Rejected on measurement

The treatment was worse, and the harness says so. These are not mistakes — they are the reason the experiment was worth running, and an optimization that only ever produced acceptable arms would be an optimization with no gate behind it.


**`A1-offline-n100-c8-rep1`** — A1, n=100, C=8

- `cascade_saw_every_query`
  <br>tier counts exceed attempted queries by exactly 8, the warm-up count -- this run predates pipe.reset_parser_stats(), which is the bug this check was written to catch. The check is working; the run is superseded.

- `name_table_recorded`
  <br>manifest has no name_table_sha256 -- predates that field. Superseded by the runs that have it.

- `parser_quality_scored`

**`A1-offline-n100-c8-rep2`** — A1, n=100, C=8

- `cascade_saw_every_query`
  <br>tier counts exceed attempted queries by exactly 8, the warm-up count -- this run predates pipe.reset_parser_stats(), which is the bug this check was written to catch. The check is working; the run is superseded.

- `name_table_recorded`
  <br>manifest has no name_table_sha256 -- predates that field. Superseded by the runs that have it.

- `parser_quality_scored`

**`A1-offline-n100-c8-rep3`** — A1, n=100, C=8

- `cascade_saw_every_query`
  <br>tier counts exceed attempted queries by exactly 8, the warm-up count -- this run predates pipe.reset_parser_stats(), which is the bug this check was written to catch. The check is working; the run is superseded.

- `name_table_recorded`
  <br>manifest has no name_table_sha256 -- predates that field. Superseded by the runs that have it.

- `parser_quality_scored`

**`A1-offline-n1000-c8-rep1`** — A1, n=1000, C=8

- `parser_quality_scored`

**`A1-offline-n1000-c8-rep2`** — A1, n=1000, C=8

- `parser_quality_scored`

**`A2-short-offline-n1000-c8-rep1`** — A2-short, n=1000, C=8

- `parser_quality_scored`

**`A2-short-offline-n1000-c8-rep2`** — A2-short, n=1000, C=8

- `parser_quality_scored`

**`A2-terse-offline-n1000-c8-rep1`** — A2-terse, n=1000, C=8

- `parser_quality_scored`

- `advisor_topic_coverage_non_inferior`

**`B0-offline-n100-c1-rep1`** — B0, n=100, C=1

- `parser_quality_scored`

**`B0-offline-n100-c1-rep2`** — B0, n=100, C=1

- `parser_quality_scored`

**`B0-offline-n100-c16-rep1`** — B0, n=100, C=16

- `parser_quality_scored`

**`B0-offline-n100-c16-rep2`** — B0, n=100, C=16

- `parser_quality_scored`

**`B0-offline-n100-c2-rep1`** — B0, n=100, C=2

- `parser_quality_scored`

**`B0-offline-n100-c2-rep2`** — B0, n=100, C=2

- `parser_quality_scored`

**`B0-offline-n100-c32-rep1`** — B0, n=100, C=32

- `parser_quality_scored`

**`B0-offline-n100-c32-rep2`** — B0, n=100, C=32

- `parser_quality_scored`

**`B0-offline-n100-c4-rep1`** — B0, n=100, C=4

- `parser_quality_scored`

**`B0-offline-n100-c4-rep2`** — B0, n=100, C=4

- `parser_quality_scored`

**`B0-offline-n100-c64-rep1`** — B0, n=100, C=64

- `parser_quality_scored`

**`B0-offline-n100-c64-rep10`** — B0, n=100, C=64

- `parser_quality_scored`

**`B0-offline-n100-c64-rep2`** — B0, n=100, C=64

- `parser_quality_scored`

**`B0-offline-n100-c64-rep9`** — B0, n=100, C=64

- `parser_quality_scored`

**`B0-offline-n100-c8-rep1`** — B0, n=100, C=8

- `parser_quality_scored`

**`B0-offline-n100-c8-rep2`** — B0, n=100, C=8

- `parser_quality_scored`

**`B0-offline-n100-c8-rep20`** — B0, n=100, C=8

- `parser_quality_scored`

**`B0-offline-n1000-c8-rep1`** — B0, n=1000, C=8

- `parser_quality_scored`

**`B0cal-offline-n100-c1-rep1`** — B0cal, n=100, C=1

- `parser_quality_scored`

## Advisor arms judged against a reference supplied at recheck

These arms were compared by hand when they ran, because the `--advisor-reference` flag did not exist yet. The comparison below is made by `validity.check_run` from briefing scores frozen at run time; only the *choice of reference* was supplied afterwards, and their manifests still record that they declared none.

- `A2-short-offline-n1000-c8-rep1` vs `results/A1-offline-n1000-c8-rep2` — **REJECTED** (parser_quality_scored)
- `A2-short-offline-n1000-c8-rep2` vs `results/A1-offline-n1000-c8-rep2` — **REJECTED** (parser_quality_scored)
- `A2-terse-offline-n1000-c8-rep1` vs `results/A1-offline-n1000-c8-rep2` — **REJECTED** (parser_quality_scored, advisor_topic_coverage_non_inferior)
